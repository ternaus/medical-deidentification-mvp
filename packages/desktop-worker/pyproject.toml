[project]
name = "medical-deid-worker"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
build = ["pyinstaller>=6.15"]

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/medical_deid_worker"]
